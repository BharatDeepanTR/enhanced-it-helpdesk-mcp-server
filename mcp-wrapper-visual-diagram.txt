```
🔄 MCP WRAPPER LAMBDA - VISUAL FLOW DIAGRAM
===========================================

📊 CURRENT PROBLEM:
┌─────────────────┐    MCP Request     ┌─────────────────────────────┐
│ Bedrock Gateway │ ──────────────────→ │ Your Original Lambda        │
│                 │ {"jsonrpc":"2.0",  │ a208194-chatops_application │
│                 │  "method":"tools/  │ _details_intent             │
│                 │   list"}           │                             │
└─────────────────┘                    └─────────────────────────────┘
                                                      │
                                                      ▼ 
                                         ❌ UnknownOperationException
                                            (Doesn't understand MCP)


🎯 SOLUTION WITH MCP WRAPPER:
┌─────────────────┐    MCP Request     ┌─────────────────┐    JSON Request    ┌─────────────────────────────┐
│ Bedrock Gateway │ ──────────────────→ │ MCP Wrapper     │ ──────────────────→ │ Your Original Lambda        │
│                 │                    │ Lambda          │                    │ a208194-chatops_application │
│                 │                    │                 │                    │ _details_intent             │
└─────────────────┘                    └─────────────────┘                    └─────────────────────────────┘
         ▲                                       │                                           │
         │                                       ▼                                           ▼
         │                              ┌─────────────────┐                     ┌─────────────────────────────┐
         │                              │ Protocol        │                     │ Regular JSON Response       │
         │                              │ Translation:    │                     │ {"application": "MyApp",    │
         └──────────────────────────────│ • Parse MCP     │◄────────────────────│  "contact": "admin@...",   │
                 MCP Response           │ • Call Lambda   │                     │  "regions": [...]}          │
                                        │ • Format Result │                     │                             │
                                        └─────────────────┘                     └─────────────────────────────┘


📋 DETAILED MCP REQUEST/RESPONSE FLOW:

1️⃣  TOOLS/LIST REQUEST:
    ┌─────────────────┐
    │ Gateway         │ ──► {"jsonrpc": "2.0", "method": "tools/list", "params": {}}
    └─────────────────┘
                            ▼
    ┌─────────────────┐     ┌─────────────────────────────────────────────────┐
    │ MCP Wrapper     │ ──► │ Returns hardcoded tool definition:             │
    │ - Receives MCP  │     │ {                                               │
    │ - Recognizes    │     │   "result": {                                  │
    │   "tools/list"  │     │     "tools": [{                                │
    │ - Returns tools │     │       "name": "get_application_details",       │
    └─────────────────┘     │       "description": "Get app details...",     │
                            │       "inputSchema": {...}                     │
                            │     }]                                          │
                            │   }                                             │
                            └─────────────────────────────────────────────────┘

2️⃣  TOOLS/CALL REQUEST:
    ┌─────────────────┐
    │ Gateway         │ ──► {"jsonrpc": "2.0", "method": "tools/call", 
    └─────────────────┘      "params": {"name": "get_application_details", 
                                       "arguments": {"asset_id": "a208194"}}}
                            ▼
    ┌─────────────────┐     ┌─────────────────────────────────────────────────┐
    │ MCP Wrapper     │     │ 1. Extract arguments: {"asset_id": "a208194"}  │
    │ - Parse request │     │ 2. Invoke target Lambda with arguments         │
    │ - Extract args  │ ──► │ 3. Get response: {"application": "MyApp",...}   │
    │ - Call target   │     │ 4. Wrap in MCP format:                         │
    │ - Format result │     │    {"result": {"content": [{"type": "text",    │
    └─────────────────┘     │                "text": "JSON response"}]}}     │
                            └─────────────────────────────────────────────────┘


🔧 CODE FLOW BREAKDOWN:

┌─────────────────────────────────────────────────────────────────────────────────┐
│ def lambda_handler(event, context):                                             │
│     ┌─ INPUT PROCESSING ────────────────────────────────────────────────────┐   │
│     │ # Handle different event formats                                      │   │
│     │ if 'body' in event:                                                   │   │
│     │     request_body = json.loads(event['body'])  # API Gateway format   │   │
│     │ else:                                                                  │   │
│     │     request_body = event                      # Direct invocation     │   │
│     └───────────────────────────────────────────────────────────────────────┘   │
│                                        │                                         │
│     ┌─ MCP PARSING ─────────────────────▼─────────────────────────────────┐   │
│     │ method = request_body.get('method')      # "tools/list" or "tools/call"│   │
│     │ params = request_body.get('params', {})  # Request parameters        │   │
│     │ request_id = request_body.get('id')      # For response correlation  │   │
│     └───────────────────────────────────────────────────────────────────────┘   │
│                                        │                                         │
│     ┌─ METHOD ROUTING ──────────────────▼─────────────────────────────────┐   │
│     │ if method == 'tools/list':                                           │   │
│     │     return hardcoded_tool_definitions()                              │   │
│     │ elif method == 'tools/call':                                         │   │
│     │     tool_name = params.get('name')                                   │   │
│     │     tool_args = params.get('arguments')                              │   │
│     │     ┌─ TARGET LAMBDA CALL ──────────────────────────────────────┐     │   │
│     │     │ lambda_client.invoke(                                     │     │   │
│     │     │     FunctionName='a208194-chatops_application_details...',│     │   │
│     │     │     Payload=json.dumps(tool_args)                        │     │   │
│     │     │ )                                                         │     │   │
│     │     └───────────────────────────────────────────────────────────┘     │   │
│     │     return mcp_formatted_response(result)                            │   │
│     └───────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘


🧪 TESTING SCENARIOS:

Test 1: Direct Wrapper Test
┌─────────────────────────────────────────────────────────────────────────────────┐
│ aws lambda invoke \                                                             │
│   --function-name mcp-wrapper-lambda \                                         │
│   --payload '{"jsonrpc":"2.0","method":"tools/list","params":{}}' \            │
│   response.json                                                                │
│                                                                                 │
│ Expected: {"jsonrpc":"2.0","result":{"tools":[{"name":"get_application_..."}]}}│
└─────────────────────────────────────────────────────────────────────────────────┘

Test 2: Gateway Integration Test
┌─────────────────────────────────────────────────────────────────────────────────┐
│ curl -X POST https://gateway.../mcp \                                          │
│   -H "Authorization: AWS4-HMAC-SHA256 ..." \                                   │
│   -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"get_app...",    │
│        "arguments":{"asset_id":"a208194"}}}'                                   │
│                                                                                 │
│ Expected: {"jsonrpc":"2.0","result":{"content":[{"type":"text","text":"..."}]}}│
└─────────────────────────────────────────────────────────────────────────────────┘


🎯 KEY BENEFITS:

✅ Protocol Compatibility:  Your Lambda now speaks MCP language
✅ Zero Code Changes:       Original Lambda remains unchanged  
✅ Tool Discovery:          Agents can find available tools automatically
✅ Error Handling:          Proper MCP error format for debugging
✅ Future-Proof:           Easy to add more tools/functions
✅ Bedrock Integration:     Full compatibility with Agent Core Gateway

🚀 RESULT: 
Your existing Lambda function works seamlessly with Bedrock's Agent ecosystem!
```