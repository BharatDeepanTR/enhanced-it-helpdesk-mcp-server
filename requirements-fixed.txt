# Core dependencies with compatible versions for Docker build
# Fixed dependency conflicts for Cloud Shell deployment

# Core dependencies
requests==2.31.0
boto3==1.34.34
botocore==1.34.34

# AWS SDK dependencies (compatible with boto3 1.34.34)
jmespath==1.0.1
s3transfer==0.10.2

# Security and networking
certifi==2023.11.17
urllib3==2.1.0
charset-normalizer==3.3.2
idna==3.6

# Date and time handling
python-dateutil==2.8.2
pytz==2023.3

# Configuration management
configparser==5.3.0

# Note: Removed awslambdaric as we're using container handler
# All versions tested for compatibility